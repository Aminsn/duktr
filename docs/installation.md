# Installation

You can install `duktr` from PyPI using `pip`:

```bash
pip install duktr
```

## Development Installation

If you want to contribute to the development of `duktr`, you can install it from the source code:

```bash
git clone https://github.com/your-username/duktr.git
cd duktr
pip install -e .
```
